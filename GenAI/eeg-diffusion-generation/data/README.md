# Local Data

Place EEG arrays, generated samples, EFDM outputs, and other local datasets here.

The original workspace included many `.pickle` files and generated-data directories. They are intentionally excluded from Git.
