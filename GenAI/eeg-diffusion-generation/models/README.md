# Local Model Artifacts

Place restored TensorFlow checkpoints, Keras weights, and PyTorch model files here.

This directory is intentionally ignored by Git because the original model artifacts are large.
